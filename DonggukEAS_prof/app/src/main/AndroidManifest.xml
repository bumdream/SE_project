<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dgu.donggukeas_prof">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AttendanceActivity" />
        <activity
            android:name=".ui.QrActivity"
            android:label="QR Code"
            android:theme="@style/MyAlertDialogStyle" />
        <activity android:name=".ui.AttendanceMenuActivity" />
        <activity android:name=".ui.RunawayActivity"></activity>
    </application>

</manifest>